struct Point {
    i32 x,
    i32 y,
}

struct Rect {
    Point a,
    Point b,
}

fn test(){
    println("hello from test");
}

fn tt(i64 b):i64{
    let(i,0,i64);
    warpto(start);
    point start; 
    warptoif(<(i,b),loop1,end);
    point loop1;
        #=(j,0,i64);
        warpto(loop2start);
        point loop2start;
        warptoif(<(j,b),loop2,loop2end);
        point loop2;
            println(format("i = %lld,j = %lld",i,j));
            =(j,+(j,1));
            warpto(loop2start)
        point loop2end;
            =(i,+(i,1));
            warpto(start);
    point end;
        return 0;
}